import { ColorNumberPipe } from './color-number.pipe';

describe('ColorNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
